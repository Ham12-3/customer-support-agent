# Backend Environment Variables Template
# 
# FOR .NET USER SECRETS (Recommended):
# Copy the commands below and run them in PowerShell
# Or run: .\setup-dotnet-secrets.ps1
#
# FOR .ENV FILE:
# Copy this file to .env and fill in your actual values
# Then install DotNetEnv package and load in Program.cs

# ============================================
# Gemini AI Configuration
# ============================================
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-3-flash
GEMINI_MAX_TOKENS=2048
GEMINI_TEMPERATURE=0.7

# User Secrets Command:
# dotnet user-secrets set "Gemini:ApiKey" "YOUR_ACTUAL_API_KEY"

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=customersupport
DB_USER=postgres
DB_PASSWORD=postgres

# User Secrets Command:
# dotnet user-secrets set "ConnectionStrings:DefaultConnection" "Host=127.0.0.1;Database=customersupport;Username=postgres;Password=YOUR_PASSWORD;Port=5432;SSL Mode=Disable;Timeout=30;"

# ============================================
# Redis Configuration
# ============================================
REDIS_CONNECTION=localhost:6379

# User Secrets Command:
# dotnet user-secrets set "ConnectionStrings:Redis" "localhost:6379"

# ============================================
# JWT Authentication
# ============================================
# Generate a strong random secret (at least 64 characters)
JWT_SECRET=your_very_strong_jwt_secret_at_least_64_characters_long_here
JWT_ISSUER=CustomerSupportAgent
JWT_AUDIENCE=CustomerSupportAPI
JWT_EXPIRATION_MINUTES=60

# User Secrets Command:
# dotnet user-secrets set "JWT:Secret" "YOUR_STRONG_SECRET"

# ============================================
# Refresh Token Configuration
# ============================================
REFRESH_TOKEN_DAYS_TO_EXPIRE=30
REFRESH_TOKEN_MAX_ACTIVE_PER_USER=5

# ============================================
# CORS Configuration
# ============================================
# Development CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# For production, update to your actual domains:
# CORS_ORIGINS=https://your-dashboard.com,https://your-widget-domain.com

# ============================================
# API URLs
# ============================================
API_BASE_URL=http://localhost:5000
WIDGET_URL=http://localhost:3001

# ============================================
# Environment Mode
# ============================================
ASPNETCORE_ENVIRONMENT=Development

# For production:
# ASPNETCORE_ENVIRONMENT=Production

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL_DEFAULT=Information
LOG_LEVEL_ASPNETCORE=Warning
LOG_LEVEL_ENTITYFRAMEWORK=Warning

# ============================================
# Features
# ============================================
FEATURE_EMAIL_NOTIFICATIONS=false

# ============================================
# QUICK SETUP
# ============================================
# Option 1: User Secrets (Recommended)
#   Run: .\setup-dotnet-secrets.ps1
#
# Option 2: .env File
#   1. Copy this file to backend/.env
#   2. Fill in your actual values
#   3. Run: cd backend/src/CustomerSupport.Api && dotnet add package DotNetEnv
#   4. Add to Program.cs: Env.Load("../../.env");
#
# See DOTNET_SECRET_MANAGEMENT.md for detailed instructions

